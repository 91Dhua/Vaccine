# 修改日志 - 2026-06-29 - 物料详情删除与批次文案

## 变更日期

2026-06-29

## 修改原因

物料详情页顶部状态标签和说明文案占用注意力，且当前用户希望操作口径从 `停用` 调整为 `删除`。批次库存表中 `批次号` 文案也需要更贴近库存语境，调整为 `库存批次`。

## 受影响文件

- `src/components/inventory/ConsoleInventoryPage.tsx`

## 用户可见变化

- 物料详情页顶部不再展示 `启用中` 状态标签。
- 移除 `物料状态 · 批次库存 · 库存变化追溯` 说明文案。
- 详情页右上角 `停用` 按钮调整为 `删除物料`。
- 删除确认弹窗文案同步改为删除口径，保留“库存、批次和历史记录仍会保留”的提示。
- 批次库存表头 `批次号` 调整为 `库存批次`。

## PRD / Mock 数据更新

- 本次为前端展示与操作文案调整，不新增 mock 数据。

## 验证

- 已运行 `npm run build`，类型检查与生产构建通过。
